# update IFACE and MAC_TO_SPOOF
# iface is the interface on which the arps are inspected and replied on
# mac_to_spoof is the mac that will be used
# see --help for more options
# a negative grace period will disable auto-detect
ARPPROXY_OPTS="-i IFACE -mac MAC_TO_SPOOF -grace -10m"
